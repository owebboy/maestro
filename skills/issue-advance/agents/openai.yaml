interface:
  display_name: "Issue Advance"
  short_description: "Promote a reviewed item to tracked."
  default_prompt: "Use $issue-advance to promote this reviewed work item to a tracked item with a spec and plan."

policy:
  allow_implicit_invocation: false
