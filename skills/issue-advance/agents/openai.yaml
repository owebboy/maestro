interface:
  display_name: "Issue Advance"
  short_description: "Turn a reviewed issue into a track."
  default_prompt: "Use $issue-advance to convert this reviewed issue into a Maestro track."

policy:
  allow_implicit_invocation: false
