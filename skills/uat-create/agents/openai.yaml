interface:
  display_name: "UAT Create"
  short_description: "Create UAT checklists from completed work items."
  default_prompt: "Use $uat-create to generate a UAT checklist from completed Maestro work items."

policy:
  allow_implicit_invocation: false
