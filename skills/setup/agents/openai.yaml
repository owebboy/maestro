interface:
  display_name: "Setup"
  short_description: "Initialize .maestro context + backend."
  default_prompt: "Use $setup to initialize Maestro .maestro/ context and adapter for this repository."

policy:
  allow_implicit_invocation: false
