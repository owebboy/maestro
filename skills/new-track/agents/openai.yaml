interface:
  display_name: "New Track"
  short_description: "Create a tracked item with spec and plan."
  default_prompt: "Use $new-track to create a tracked work item with a spec and implementation plan."

policy:
  allow_implicit_invocation: false
