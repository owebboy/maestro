interface:
  display_name: "New Track"
  short_description: "Create a tracked spec and plan."
  default_prompt: "Use $new-track to create a Maestro track with a spec and implementation plan."

policy:
  allow_implicit_invocation: false
