interface:
  display_name: "Session Wrap-Up"
  short_description: "Review session work, update CLAUDE.md/AGENTS.md, capture follow-ups, and commit."
  default_prompt: "Use $session-wrap-up to review session work, capture follow-ups, and prepare commits."

policy:
  allow_implicit_invocation: false
