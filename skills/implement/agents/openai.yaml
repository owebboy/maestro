interface:
  display_name: "Implement Track"
  short_description: "Execute a planned conductor track, or implement a small issue file directly."
  default_prompt: "Use $implement to execute the active Maestro track plan with checkpoints, or pass an issues/*.md path to implement a small issue directly."

policy:
  allow_implicit_invocation: false
