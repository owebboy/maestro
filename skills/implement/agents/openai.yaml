interface:
  display_name: "Implement Track"
  short_description: "Execute a planned conductor track."
  default_prompt: "Use $implement to execute the active Maestro track plan with checkpoints."

policy:
  allow_implicit_invocation: false
