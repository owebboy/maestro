interface:
  display_name: "Implement"
  short_description: "Implement a work item (tracked plan or light fix)."
  default_prompt: "Use $implement to execute a tracked item's plan with checkpoints, or fix a light item directly."

policy:
  allow_implicit_invocation: false
