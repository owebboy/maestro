interface:
  display_name: "Triage"
  short_description: "Convert inbox bullets into work items."
  default_prompt: "Use $triage to convert .maestro/inbox.md bullets into structured work items."

policy:
  allow_implicit_invocation: false
