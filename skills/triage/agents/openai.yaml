interface:
  display_name: "Triage"
  short_description: "Convert INBOX bullets into issues."
  default_prompt: "Use $triage to convert issues/INBOX.md bullets into structured issue files."

policy:
  allow_implicit_invocation: false
