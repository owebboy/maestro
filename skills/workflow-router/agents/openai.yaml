interface:
  display_name: "Workflow Router"
  short_description: "Choose the right Maestro workflow."
  default_prompt: "Use $workflow-router to choose the right Maestro workflow for this task."

policy:
  allow_implicit_invocation: true
