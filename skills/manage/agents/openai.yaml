interface:
  display_name: "Track Manager"
  short_description: "Manage conductor track lifecycle."
  default_prompt: "Use $manage to list, archive, restore, rename, delete, or clean up Maestro tracks."

policy:
  allow_implicit_invocation: false
