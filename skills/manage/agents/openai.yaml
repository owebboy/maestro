interface:
  display_name: "Work Item Manager"
  short_description: "Manage work-item lifecycle."
  default_prompt: "Use $manage to archive, restore, rename, delete, or clean up Maestro work items."

policy:
  allow_implicit_invocation: false
