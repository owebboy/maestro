interface:
  display_name: "Issue Close"
  short_description: "Archive an issue without a track."
  default_prompt: "Use $issue-close to archive this issue without creating a Maestro track."

policy:
  allow_implicit_invocation: false
