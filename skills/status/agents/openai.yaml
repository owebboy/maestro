interface:
  display_name: "Status"
  short_description: "Show work items and next actions."
  default_prompt: "Use $status to show current Maestro work item board and next actions."

policy:
  allow_implicit_invocation: true
